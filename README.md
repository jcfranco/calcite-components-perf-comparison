# Native vs. Calcite table performance

Vanilla JavaScript reproduction that renders the same 1,200 rows as either a
native HTML table or a Calcite table. It has no Angular or ArcGIS Maps SDK
dependencies.

```sh
npm install
npm start
```
